You are a senior Laravel and React architect.

I have a fresh Laravel 12 project.

Your task is ONLY to setup the project foundation.

Requirements:

Backend:

* Laravel 12
* PHP 8.4

Frontend:

* Inertia.js v2
* React 19
* TypeScript

Styling:

* Tailwind CSS v4

Rules:

* Do NOT create any business modules.
* Do NOT create authentication pages.
* Do NOT create dashboard pages.
* Do NOT install unnecessary packages.

Tasks:

1. Install and configure Inertia.js React.
2. Configure TypeScript.
3. Configure Tailwind CSS v4.
4. Setup folder structure for scalable enterprise application.
5. Create reusable layouts folder.
6. Create reusable components folder.
7. Create reusable hooks folder.
8. Create reusable services folder.
9. Create reusable types folder.
10. Create path aliases.

Expected output:

* List of packages installed.
* Commands executed.
* Folder structure.
* Complete code changes.
* Explanation of every file created.
* Wait for my approval before continuing.
